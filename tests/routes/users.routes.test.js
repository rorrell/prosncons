const expect = require('chai').expect
  , routes = require('../../controllers/users.controller')

describe('The user routes', () => {
  it('Should return 200 for GET /register', () => {

  })
})