const expect = require('chai').expect
  , controller = require('../../controllers/index.controller')

describe('The index routes', () => {
  it('Should return 200 for GET /', () => {

  })
  it('Should return 200 for GET /about', () => {

  })
})